{
	"name": "mud-command",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"build": "tsc",
		"test": "node --test \"dist/**/*.spec.js\"",
		"coverage": "node --test --experimental-test-coverage \"dist/**/*.spec.js\"",
		"deplay": "npm run build && npm run test && npm run coverage",
		"doc": "npx typedoc src/*.ts"
	},
	"devDependencies": {
		"@types/node": "^24.9.1",
		"typedoc": "^0.28.14",
		"typescript": "^5.5.0"
	},
	"dependencies": {
		"mud-ext": "github:jackindisguise/mud-ext"
	}
}
